# 우주 방어 기술 설계

## 선택과 경계

제품 수치·완료 기준은 [PRD](PRD.md), 고정 자산 출처·해시는 [ideation](ideation.md)를 참조한다.
위임 설계 검토로 HTML/CSS·ES Modules, Canvas 2D, Vite와 작은 모델/UI 분리를 선택했다.
그래픽 엔진·과도한 계층·고정 내부 함수명은 도입하지 않는다.
현재 Node 24 LTS 환경에 호환되는 안정판을 구현 때 확인해 manifest에 먼저 명시한 뒤 설치하고 lockfile을 커밋한다.
실제 npm script 명령은 구현 단계 manifest에서 확정한다.

## 책임과 처리 흐름

| 책임 | 설계 | PRD 연결 |
|---|---|---|
| 모델 | DOM 없이 상태·플레이어·탄환·편대·방향·점수·발사 대기·시간을 소유한다. 초 단위 delta와 입력을 받아 갱신한다. | GAME-01~08 |
| 상태 전환 | title/playing/won/lost별 허용 동작을 명시한다. 종료 상태에서 시뮬레이션은 갱신하지 않는다. 새 게임은 새 초기 상태와 입력·시간을 만든다. | GAME-01,06~08 |
| 갱신 순서 | 이동·발사·탄환·편대 갱신 후 strict 사각형 충돌, 제거/점수, 적 소진 승리, 남은 적의 방어선 패배 순으로 처리한다. 제거 대상의 중복 소비를 막는다. | GAME-02~06 |
| 브라우저 연결 | 단일 requestAnimationFrame 루프와 한 번만 연결하는 리스너. 긴 프레임 delta를 상한 처리해 과도한 이동·시간 점프를 막는다. 전환 시 시각 기준을 정리한다. | GAME-07~08 |
| 입력 | 눌린 키 집합과 단발 명령을 분리한다. 반대 방향은 상쇄하며 단발 repeat는 무시한다. blur·상태 전환 시 입력을 비운다. | INPUT-01~02 |
| 표시 | Canvas는 모델의 읽기 전용 표현, HUD는 HTML 실제 텍스트다. 상태 변경 시 모델과 DOM을 동기화하고 최초 값은 첫 rAF 전에 맞춘다. | UI-01~02 |

## 이미지·HUD

해당 단계의 PNG를 로컬 정적 import하고 Vite 생성 URL을 사용한다.
초기화 때 한 번 로드·디코딩하고 모든 필수 이미지 준비 전 시작 버튼과 시작 키를 차단한다.
거부된 로드·디코딩은 사용자에게 오류로 표시하고 시작 불가를 유지한다. 임의 도형으로 숨기지 않는다.
Canvas drawImage로 원본 비율을 유지하고 ideation의 모델 기준 정렬을 따른다.
그림 외곽·투명 영역을 물리·충돌·탄환 출발점의 기준으로 사용하지 않는다.
HUD 카드는 고정된 동일 크기와 여백, 숫자 고정폭을 사용하고 조작·상태 영역과 분리한다.
미구현 후속 기능의 빈 카드를 넣지 않는다.

## 검증 경로

Node 내장 `node --test`는 제품 모델을 직접 import하고 제어 delta로 경계·출발점·발사 간격·
충돌·점수 중복·승패 우선순위·종료 동결·재시작을 검사한다.
Playwright Chromium은 같은 제품의 실제 DOM·키·버튼·Canvas·포커스·네트워크를 확인한다.
승패 같은 모델 경계와 실제 브라우저 입력 연결을 서로의 대체 증거로 삼지 않는다.
테스트 전용 제품 글로벌 모델 API·치트키는 금지한다.
지연·실패 이미지는 테스트의 route로 재현하고 모델에 실패 주입 코드를 넣지 않는다.
Canvas 검사는 알파가 0인 픽셀을 기체 색으로 세지 않고 다색 PNG·래스터와 모델 좌표 차이를 고려한다.
제어 시계 테스트는 시작 시점 경합을 피하며 시간 초과를 유한 단계로 제한한다.
공개 자연 시간 관찰은 clock/모델 주입 자동 검사와 명확히 구분해 기록한다.
검증 항목과 실제 결과는 05에서 TEST_PLAN/TEST_RESULTS에 연결한다.

## 실행·배포·운영

Vite base는 `./`, dev/E2E는 5173, preview는 4173을 사용한다.
시작 세션이 PID·URL·응답을 확인하고 소유 프로세스만 관리·종료한다. 충돌 시 미상 프로세스를 죽이지 않는다.
빌드 dist만 Pages에 배포한다. root·저장소 하위 경로에서 실제 이미지 디코딩·표시와 외부 요청 부재를 확인한다.
작은 PNG가 data URL로 인라인될 수 있으므로 dist의 특정 PNG 파일 수를 강제하지 않는다.
파일 URL인 자산은 응답·MIME·404도 확인한다.
06에서 최소 권한 Actions/Pages main 배포를 설계하고 CI의 외부 미러 재다운로드를 금지한다.
이번 단계에는 코드·manifest·설치·.github 설정을 생성하지 않는다.
