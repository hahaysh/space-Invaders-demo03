# 우주 방어 제품 요구사항

## 범위와 기준

대상과 선택 이유는 [ideation](ideation.md), 작업 방식은 [AGENTS](AGENTS.md)를 따른다.
아래는 첫 배포 제품의 단일 수치 기준이다. 좌표 원점은 Canvas 왼쪽 위, 위치는 모델 사각형의 왼쪽 위,
시간은 초, 속도는 px/s이다. 사용자 위임으로 추천 기본값을 검토·선택했다.

| 항목 | 기본값 |
|---|---|
| 화면·플레이어 | 800×600, 모델 40×20, 초기 (380,550), 속도 320 |
| 플레이어 탄환 | 4×12, 위로 속도 600, 발사 간격 0.2초, 화면 밖 제거 |
| 탄환 출발 | 플레이어 수평 중앙, 탄환 바닥 = 플레이어 위쪽; 초기 (398,538) |
| 적 편대 | 3행×8열=24, 모델 40×24, (112+72×열,72+48×행) |
| 적 이동 | 초기 오른쪽, 속도 64, 가장자리 방향 반전과 24 하강 |
| 점수·방어선 | 적 하나 10점, 최대 240점, 적 바닥이 y=520에 닿으면 패배 |

## 기능 수용 기준

| ID | 요구·완료 조건 | 확인 방식 |
|---|---|---|
| GAME-01 | 초기 title에서 제목·조작·점수 0·시작 버튼을 표시한다. 이미지 준비 후 버튼 또는 nonrepeat Enter로 playing 진입한다. | 초기 DOM, 키·버튼 |
| GAME-02 | playing에서 좌우 화살표/A·D로 이동하고 모델 전체가 화면 경계 안에 남는다. 반대 방향 동시 입력은 정지한다. | 모델 경계·실제 키 |
| GAME-03 | playing에서 Space를 누르는 동안 기준 간격으로 위로 발사한다. 화면 밖 탄환을 제거하고 게임 조작에 필요한 범위만 기본 스크롤을 막는다. | 모델 간격·출발점·실제 키 |
| GAME-04 | 살아 있는 편대가 기준 속도로 이동하고 좌우 가장자리에서 한 번 반전·하강한다. | 모델·Canvas 진행 |
| GAME-05 | 충돌은 strict rectangle overlap(변끼리 닿기만 하면 충돌 아님)이다. 탄환 하나는 적 하나만 제거하고 이미 제거한 적은 중복 점수를 내지 않는다. | 경계·중복 자동 검사 |
| GAME-06 | 한 갱신에서 이동·충돌 처리 후 적이 없으면 won, 아니면 방어선 도달 시 lost. 마지막 적 제거와 방어선 도달이 겹치면 won이 우선이다. | 우선순위 자동 검사 |
| GAME-07 | won/lost에서 이동·탄환·적·시간·점수가 멈추고 결과와 재시작 버튼을 보인다. 버튼 또는 nonrepeat R만 새 게임을 시작한다. | 모델 동결·실제 키·버튼 |
| GAME-08 | 새 게임은 점수·적 편대·방향·플레이어·탄환·시간·발사 대기·입력을 모두 초기화한다. 반복 재시작에도 루프가 중복되지 않는다. | 초기 상태 비교·반복 조작 |
| INPUT-01 | 시작·재시작의 key repeat를 무시한다. 상태 전환·blur/포커스 이탈 시 입력을 비운다. blur 자동 일시정지는 없다. | 실제 키·포커스·모델 |
| INPUT-02 | title/won/lost에서 이동·발사를 무시하고 playing에서는 시작·재시작을 무시한다. 다른 UI 입력을 불필요하게 가로채지 않는다. | 상태별 키 검사 |
| UI-01 | 짙은 남색 둥근 상단 패널과 조금 밝은 카드·은은한 테두리, 작은 라벨/크고 굵고 밝은 값을 HTML/CSS 텍스트로 제공한다. 첫 버전은 점수 카드만이다. | DOM·computed style·화면 |
| UI-02 | 카드 크기·안쪽 여백·간격을 일정하게 유지하고 0→240 변화에 위치가 밀리지 않는다. 숫자는 고정폭이다. 버튼·상태 안내는 카드 밖에 둔다. PC 창 크기 변경에서 잘림·겹침이 없어야 한다. | DOM 사각형 측정·화면 |
| ASSET-01 | ideation의 고정 CC0 PNG 정확히 두 개와 포함 라이선스를 무수정으로 보존하고 SHA-256을 대조한다. M1은 플레이어, M2는 적을 추가한다. | 파일 해시·크기 |
| ASSET-02 | 그림 너비 40, 높이 40×원본높이/원본너비. 플레이어는 모델 왼쪽·위, 적은 왼쪽·아래에 정렬한다. 모델 수치·경계·탄환 출발·방어선은 기준표를 유지한다. | 모델 검사·drawImage·실제 표시 |
| ASSET-03 | 위를 향한 청색 플레이어와 아래를 향한 적갈색 적의 몸체·날개·조종석과 배경 대비를 실제 크기에서 구별한다. 그림·투명 영역은 충돌 모델과 다르며 픽셀 피격은 없다. | 실제 Canvas 화면·알파 고려 검사 |
| ASSET-04 | 단계별 모든 이미지의 로드·디코딩 완료 전에 시작을 차단한다. 실패 시 명시적 오류와 시작 불가를 표시한다. 도형 fallback·런타임 외부 hotlink/CDN은 금지한다. | 지연·실패 route와 실제 UI |
| RELEASE-01 | 로컬 root·저장소 하위 경로·공개 URL에서 이미지 디코딩/표시, 요청 오류와 외부 이미지 요청 부재를 확인한다. Vite data URL도 정상이다. 파일 URL이면 응답·MIME·404를 검사한다. | build·네트워크·공개 브라우저 |
| DOC-01 | README에 실행·빌드·조작·공개 주소와 Kenney/팩/고정 출처/사용 파일/CC0/라이선스 경로를 제공한다. 검증 문서에는 실제 실행과 실패·미확인을 나눈다. | 문서·실행 결과 |

## 제외 및 후속

첫 버전에는 일시정지·난이도·목숨을 구현하지 않는다. 07/08/09에서 별도 요청·설계로 추가한다.
모바일 조작·터치·모바일 검증, 적 탄환·피격 시스템, 로그인, 사운드, 최고점수, 웨이브는 제외한다.
테스트용 제품 전역 모델 API나 치트키를 제공하지 않는다.
사람의 직접 플레이, App trust·Run UI, 지침 자동 로딩은 직접 관찰하지 않으면 미확인이다.
